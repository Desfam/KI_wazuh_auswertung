# Tauri Shell

This folder is prepared for a Tauri wrapper around the Vite frontend. The current scaffold is intentionally minimal so the MVP can start with the backend and frontend immediately.

## Planned integration

- bundle the frontend build output
- run the FastAPI backend as a sidecar or local service
- store production secrets in OS-backed secure storage instead of plaintext
